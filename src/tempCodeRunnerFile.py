aths = gb.trace_to_all_exchanges(start_wallet, exchanges, cutoff=6)

    # for ex, p_list in paths.items():
    #     print(f"\nExchange: {ex}")
    #     for p in p_list:
    #         print(" -> ".join(p))
    # print()
